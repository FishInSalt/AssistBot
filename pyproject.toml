[project]
name = "assistbot"
version = "0.1.0"
description = "Telegram-based intelligent news aggregation and analysis bot"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
